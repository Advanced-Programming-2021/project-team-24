changeZone(this,my_monster);
if(#Norm(filter("cardHolderId":$my_hand$,"cardType":"MONSTER"))#>#0#)
(    
    q_yn(do_you_want_to_special_summon_another_monter?)
    (
        changeZone(selective(filter("cardHolderId":$my_hand$,"cardType":"MONSTER")),my_monster,SPECIAL_SUMMON);
    )
    (
    )
)
(
    
)
